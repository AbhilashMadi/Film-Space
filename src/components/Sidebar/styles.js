import { makeStyles } from "@mui/styles";

export default makeStyles(() => ({
  imgLink: {
    display:"flex",
    justifyContent:"center",
    padding:"10% 0"
  },

  image: {
    width: "70%"
  }
}));
